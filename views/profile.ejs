<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Helply - User Profile</title>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
        
        <style>
            /* ======================================================
            HELPLY PROFILE PAGE THEME
            ------------------------------------------------------
            Primary (Teal): #567c8d (Dominant, Background Accent)
            Secondary (Navy): #2f4156 (Strong Contrast, Buttons, Text)
            Accent (Sky Blue): #c8d9e6 (Subtle Highlight)
            Light Background: Beige #f5efeb (Soft Container Background)
            Base Background: White #ffffff
            ======================================================
            */
            
            :root {
                --primary: #567c8d;           /* Teal */
                --primary-dark: #2f4156;      /* Navy */
                --accent: #c8d9e6;            /* Sky Blue */
                --light: #f5efeb;             /* Beige */
                --white: #ffffff;             /* Pure White */
                --dark: #212529;              /* Near Black */
                --gray: #6c757d;
                --border-radius: 12px;
                --box-shadow: 0 6px 16px rgba(0, 0, 0, 0.08);
                --transition: all 0.2s ease;
            }

            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }

            body {
                font-family: 'Inter', sans-serif;
                line-height: 1.6;
                color: var(--dark);
                background-color: var(--white); /* Base background is White */
                min-height: 100vh;
            }

            .profile-container {
                max-width: 1400px;
                margin: 0 auto;
                padding: 0;
                display: flex;
                flex-direction: column;
            }

            /* Header (Top Banner Area) */
            .profile-header {
                background: var(--primary-dark); /* Navy Header */
                color: var(--white);
                padding: 2rem 0;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            }

            .header-content {
                max-width: 1200px;
                margin: 0 auto;
                padding: 0 20px;
            }

            .profile-header h1 {
                font-size: 2.5rem;
                font-weight: 700;
                color: var(--accent); /* Sky Blue accent for the main title */
            }

            /* Main Content Area */
            .profile-main {
                flex-grow: 1;
                padding: 3rem 20px;
                background-color: var(--light); /* Beige background for the main section */
            }

            .profile-card {
                display: flex;
                background: var(--white); /* White card for the content block */
                border-radius: var(--border-radius);
                box-shadow: var(--box-shadow);
                max-width: 1000px;
                margin: 0 auto;
                overflow: hidden;
            }

            /* Left Section - Avatar & Name */
            .profile-avatar-section {
                flex: 0 0 300px; /* Fixed width for the left column */
                padding: 3rem;
                background: var(--primary); /* Teal background for the avatar section */
                color: var(--white);
                text-align: center;
            }

            .profile-avatar {
                width: 120px;
                height: 120px;
                border-radius: 50%;
                object-fit: cover;
                margin-bottom: 1.5rem;
                border: 4px solid var(--accent); /* Sky Blue border around avatar */
                background-color: var(--accent); /* Placeholder background */
            }

            .profile-name {
                font-size: 1.8rem;
                font-weight: 600;
                margin-bottom: 0.5rem;
                color: var(--white);
            }

            .edit-profile-link {
                display: inline-block;
                color: var(--accent); /* Sky Blue link color */
                margin-top: 1.5rem;
                font-weight: 500;
                text-decoration: underline;
                transition: var(--transition);
            }

            .edit-profile-link:hover {
                color: var(--white);
            }

            /* Right Section - Details */
            .profile-details-section {
                flex-grow: 1;
                padding: 3rem;
            }

            .profile-details-section h3 {
                font-size: 1.5rem;
                font-weight: 600;
                margin-bottom: 1.5rem;
                color: var(--primary-dark); /* Navy heading for sections */
                border-bottom: 2px solid var(--light); /* Subtle Beige divider */
                padding-bottom: 0.5rem;
            }

            .detail-item {
                margin-bottom: 1rem;
                display: flex;
                font-size: 1.05rem;
            }

            .detail-label {
                font-weight: 600;
                color: var(--gray);
                width: 100px; /* Align labels */
            }

            .detail-value {
                color: var(--dark);
            }

            /* Action Buttons */
            .profile-actions {
                margin-top: 2.5rem;
                padding-top: 1.5rem;
                border-top: 1px solid var(--accent); /* Sky Blue divider */
                display: flex;
                gap: 1rem;
                flex-wrap: wrap;
            }

            .btn-primary, .btn-secondary {
                display: inline-block;
                padding: 0.7rem 1.4rem;
                border-radius: 6px;
                font-weight: 600;
                text-align: center;
                transition: var(--transition);
                border: 2px solid transparent;
                text-decoration: none;
                cursor: pointer;
            }

            /* Job-related actions (Primary focus for a Worker profile) */
            .btn-primary {
                background: var(--primary); /* Teal background */
                color: var(--white);
            }

            .btn-primary:hover {
                background: var(--primary-dark); /* Navy hover */
            }

            /* Task/Worker related actions (Secondary focus) */
            .btn-secondary {
                background: var(--primary-dark); /* Navy background */
                color: var(--white);
            }

            .btn-secondary:hover {
                background: var(--primary); /* Teal hover */
            }

            /* Footer */
            .profile-footer {
                background: var(--primary-dark); /* Navy Footer */
                color: var(--accent); /* Sky Blue text */
                padding: 1.5rem 20px;
                text-align: center;
                font-size: 0.9rem;
            }

            /* Responsive adjustments */
            @media (max-width: 800px) {
                .profile-card {
                    flex-direction: column;
                    max-width: 600px;
                }

                .profile-avatar-section {
                    flex: auto;
                    padding: 2rem;
                }

                .profile-details-section {
                    padding: 2rem;
                }

                .profile-actions {
                    flex-direction: column;
                    gap: 0.75rem;
                }

                .btn-primary, .btn-secondary {
                    width: 100%;
                    max-width: none;
                }
            }
        </style>
        
        </head>
    <body>
        <div class="profile-container">
            <header class="profile-header">
                <div class="header-content">
                    <h1>Your Profile</h1>
                </div>
            </header> 	
            <main class="profile-main">
                <div class="profile-card">
                    <div class="profile-avatar-section">
                        <img src="https://placehold.co/120x120/567c8d/FFFFFF?text=JD" alt="Profile Avatar" class="profile-avatar">
                        
                        <h2 class="profile-name"><%= user.Name %></h2>
                        
                        <div class="profile-bio">
                            <p></p>
                        </div>

                        <a href="#" class="edit-profile-link">Edit Profile</a>
                    </div>

                    <div class="profile-details-section">
                        <h3>Personal Information</h3>
                        <div class="detail-item">
                            <span class="detail-label">Email:</span>
                            <span class="detail-value"><%= user.email %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Phone:</span>
                            <span class="detail-value"><%= user.phone %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Location:</span>
                            <span class="detail-value">New York, USA</span>
                        </div>
                        <div class="profile-actions">
                            <a href="#" class="btn-primary">View Job Listings</a>
                            <a href="#" class="btn-primary">Apply to Jobs</a>
                            <a href="#" class="btn-secondary">Post a Job</a>
                            <a href="#" class="btn-secondary">Browse Workers</a>
                        </div>
                    </div> 		
                </div>
            </main>

            <footer class="profile-footer">
                <p>&copy; 2025 Helply. All rights reserved.</p>
            </footer>
        </div>
    </body>
</html>